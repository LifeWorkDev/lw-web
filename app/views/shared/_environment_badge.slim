- unless Rails.env.production?
  - env = Bugsnag.configuration.release_stage
  ruby:
    css_class = case env
                when 'dev' then :dark
                when 'staging' then :warning
                when 'demo' then :info
                end
  span.badge class="badge-#{css_class}" = env.humanize
